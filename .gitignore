# Geheimnisse / lokale Konfiguration — NIEMALS committen
.env
.env.*
!.env.example
*.secret
secrets/

# Mitschnitte / Rohdaten (können sensibel sein, groß)
*.pcap
*.pcapng
*.eth

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS/Editor
.DS_Store
*.swp

# Claude Code – persönliche, lokale Einstellungen (nicht teilen)
.claude/settings.local.json

# Node / Extension-Build
node_modules/
dist/
releases/

# Gateway-Laufzeitstatus
state.json
gateway/state.json

# Vom Dienst nachgeladenes Zwischenzertifikat (siehe gateway/tls.py) - es
# entsteht auf jedem Rechner neu und gehoert nicht ins Repository.
gateway/zwischenzertifikat.pem
gateway/gateway.log
# Sperrdatei des laufenden Dienstes (siehe gateway/sperre.py)
gateway/gateway.lock
# Abgelegte Monate des Protokolls (siehe gateway/protokoll.py). Darin stehen
# Namen von Personen - das gehoert nicht in ein oeffentliches Repository.
gateway/protokolle/
# Gemerkter Zugang zum Postausgang (siehe gateway/mailzugang.py). ENTHAELT DAS
# PASSWORT - darf unter keinen Umstaenden committet werden.
mailzugang.json
gateway/mailzugang.json
gateway/state.json
gateway/version.py
